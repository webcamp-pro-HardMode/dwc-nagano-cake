<div class="container">
  <h3 class="mb-5">ジャンル編集</h3>
    <%= form_with model: @genre, url: admin_genre_path, local: true do |f| %>
  <p>ジャンル名 
    <span class="mx-4"><%= f.text_field :name %></span>
    <span class="mx-4"><%= f.submit '変更を保存', class: "glyphicon glyphicon-pencil btn btn-success" %></span>
    </p>
  <% end %>
</div>